{
  "name": "kliento-verifier",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "fix": "prettier . --write",
    "static-checks": "prettier . --list-different",
    "start": "docker-compose up",
    "test": "exit 0",
    "test:functional": "vitest run server.test.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.1",
    "@veraid/kliento-verifier": "^1.0.3"
  },
  "devDependencies": {
    "@relaycorp/eslint-config": "^2.0.86",
    "@relaycorp/shared-config": "^1.9.1",
    "del-cli": "^6.0.0",
    "eslint": "^9.24.0",
    "prettier": "^3.5.3",
    "vitest": "^3.1.1"
  },
  "prettier": "@relaycorp/shared-config/.prettierrc.json",
  "engines": {
    "node": "22"
  }
}
